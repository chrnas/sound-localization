name: CI python

on:
    push:
        branches:
            - main
            - develop
    pull_request:
        branches:
            - main
            - develop
    
jobs:
    linting_flake:
        name: linting python
        runs-on: ubuntu-latest
        steps:
            - uses: actions/checkout@v4.1.1
            - name: Set up Python 3.12.2
              uses: actions/setup-python@v5
              with:
                python-version: "3.12.2"
            - name: Install dependencies
              run: |
                python -m pip install --upgrade pip
                if [ -f requirements.txt ]; then pip install -r requirements.txt; fi
            - name: Lint with flake8
              run: |
                flake8 . --count --show-source --statistics --max-complexity=10 --ignore E501
    tesing:
      name: testing python
      needs: linting_flake
      runs-on: ubuntu-latest
      steps:
          - uses: actions/checkout@v4.1.1
          - name: Set up Python 3.12.2
            uses: actions/setup-python@v5
            with:
              python-version: "3.12.2"
          - name: Install dependencies
            run: |
              python -m pip install --upgrade pip
              if [ -f requirements.txt ]; then pip install -r requirements.txt; fi
              if [ -f requirements-test.txt ]; then pip install -r requirements_test.txt; fi
          - name: Test with pytest
            run: |
              pytest
